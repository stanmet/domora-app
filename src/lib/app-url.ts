// Публичный адрес сайта. Берём из переменной APP_URL (её можно переопределить в
// Vercel), а по умолчанию - боевой домен. Так SEO, карта сайта, канонические
// ссылки и ссылки в уведомлениях работают на правильном домене даже без ручной
// настройки переменной.
export const APP_URL = (process.env.APP_URL || process.env.NEXT_PUBLIC_APP_URL || "https://domora.irish").replace(/\/$/, "");
